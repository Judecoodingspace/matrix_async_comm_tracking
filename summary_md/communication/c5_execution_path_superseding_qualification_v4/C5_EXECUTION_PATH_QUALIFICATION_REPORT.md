# C5 Q3S generated-author source-binding qualification

Q3S binds C3S `c6bb295758e343251730137b376d179c3e90e37d`, whose parent is C3R `00f2c5a5f553025f8b17bb4a5084c20f2ef838cf`. It supersedes Q3R for the Run-002 mechanical root cause only: the C5 runner now installs fixed `MIA_ROOT` and `MIA_SOURCE_ROOT` bindings after sanitization and fail-closes unless the generated PacketRuntime is byte-identical to the repository runtime and the generated entry imports, instantiates, and finalizes it.

The governed generated source is `/mnt/data/yzm/experiments/mdmt_mia_official/variants/c5_generated_author_source_binding_001`. Its runtime copy, entry, and generator manifest are individually fingerprinted in the Context. The completion validator, PacketRuntime semantics, C5 predicate, frozen cells, R values, and allowed metrics are unchanged.

Fresh synthetic/mechanical replay passed: execution gate `39 passed`, Q1–Q4 `42 passed`, combined `81 passed`. The host-only self-check passed with the qualified interpreter: two CUDA devices were visible and no scientific data was accessed.

Context and Results hashes are SHA-256 values over exact raw UTF-8 file bytes. The separate Seal payload digest is SHA-256 of sorted-key, compact-separator UTF-8 JSON. No payload contains its own final digest.

This evidence records no scientific data access, no four-cell execution, no tracking evaluation, no closed-loop intervention, and no formal scientific authorization. It makes no claim about opportunity prevalence, bytes saved, queue relief, tracking improvement, or scientific execution outcome.
