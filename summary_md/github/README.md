# GitHub CLI Experiment Management

This directory stores issue bodies for research milestones. Generated outputs,
datasets, model weights, and checkpoints remain excluded by `.gitignore`.

Repository:

```text
Judecoodingspace/matrix_async_comm_tracking
```

Current Codex sandbox does not expose the real Git worktree and its `gh` token
is invalid. Run the commands recorded in `20260801_commands.md` from the user's
authenticated terminal.

