# Work 1 Dynamic M2 Execution-Authorization Repair

## Scope

```text
ENGINEERING_VALIDITY_REPAIR_ONLY
REAL_RUNTIME_NOT_RUN
DYNAMIC_M2_NOT_AUTHORIZED_BY_THIS_REPAIR
```

No pair, delay, tracker, detector, Supplement, MIA, XML scientific value, or
Work 1 causal variable was changed.

## Repaired findings

| Review finding | Repair | Fail-closed evidence |
| --- | --- | --- |
| uniform checkpoint omission could pass | independent schema/frame/profile validation before comparison | empty, uniformly incomplete and uniformly malformed traces are negative tests |
| parent to A-traced parity absent | exact author-output artifact parity command | `PARENT_VS_A_TRACED_OUTPUT_DIFF=0` required |
| G-XML runtime evidence not producible | passive initialization state record, G-XML2 builder, G-XML3 counter artifact, complete G-XML4 boundary artifact | existing validators consume the produced schemas |
| exact determinism unresolved | isolated B repeat and exact trace repeat comparator | any mismatch is `EXACT_COMPARISON_DETERMINISM_BLOCKER` |
| launch differences only documented | normalized env/argv machine audit | unlisted or unused authorized differences fail closed |
| observer mutation count overlapped | unique non-guard positions plus unique guard phases | zero remains the required decision value; nonzero is no longer double-counted |

## Mandatory runtime order

```text
frozen parent/source hash checks
-> persistent derivative generation and structure/hash audit
-> machine launch-diff PASS
-> parent reference + A-traced + B + B-repeat + C
-> parent/A-traced output parity PASS
-> B exact-repeat PASS
-> G-XML2 PASS
-> G-XML4 PASS
-> G-XML3 dynamic PASS
-> independent A/B/C trace completeness PASS
-> CORE_OUTPUT_DIFF == 0
-> TRACKER_MUTATION_COUNT_FROM_OBSERVER == 0
```

The externally frozen full frame count is mandatory input to both the exact
repeat and A/B/C comparison commands. It cannot be inferred from a possibly
truncated trace.

## Verification performed

```text
focused synthetic/static suite = 43 passed
normalized launch-diff audit = PASS
Python AST parse = PASS
real tracker/detector/A/B/C = NOT RUN
dataset/XML scientific content = NOT READ
```

## Remaining governance action

The implementation must be committed with a clean source-provenance record
before execution authorization. Persistent derivatives and all runtime evidence
remain intentionally absent until that separate authorization.

```text
REPAIR_IMPLEMENTED
READY_FOR_FRESH_DYNAMIC_M2_EXECUTION_AUTHORIZATION_REVIEW
```
